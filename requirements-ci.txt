# ============================================================
# requirements-ci.txt
# Minimal dependencies for GitHub Actions CI.
# No CUDA, no heavy GPU libraries (xgboost uses CPU in CI).
# ============================================================

# Core ML (CPU-only — no CUDA drivers in CI)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.4.0
joblib>=1.3.0
xgboost>=2.0.0

# API layer
fastapi>=0.100.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Test client (httpx is required by TestClient in newer starlette)
httpx>=0.25.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0

# Linting & type checking
ruff>=0.3.0
pyright>=1.1.350
